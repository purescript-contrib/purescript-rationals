{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "build": "pulp build",
    "test": "pulp test",
    "spago-build": "spago build",
    "spago-test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "spago": "^0.20.3",
    "purescript": "^0.14.3",
    "big-integer": "^1.6.48"
  }
}
