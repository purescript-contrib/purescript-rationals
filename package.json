{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "spago build",
    "test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.14.0"
  }
}
