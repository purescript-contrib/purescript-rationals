{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build",
    "test": "pulp test",
    "spago-build": "spago build",
    "spago-test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.14.0"
  }
}
