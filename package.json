{
  "private": true,
  "scripts": {
    "build": "spago build",
    "spago-build": "spago build",
    "test": "spago -x ./test.dhall test",
    "spago-test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "purescript": "^0.15.8",
    "spago": "^0.20.9"
  }
}
