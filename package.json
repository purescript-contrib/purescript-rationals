{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "build": "spago build",
    "test": "spago -x ./test.dhall test"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "spago": "^0.19.1",
    "purescript": "^0.14.0",
    "big-integer": "^1.6.48"
  }
}
